export enum VERIFICATION_TOKEN {
  EMAIL = 'email',
  TEAM_INVITATION = 'team-invitation'
}

export enum EMAIL_VERIFICATION_ERRORS {
  NOT_FOUND="not-found",
  EXPIRED="expired",
  INVALID="invalid"
}
